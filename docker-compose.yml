# version: '3'
# services:
#   web:
#     build: .
#     ports:
#     - "80:80"
#     - "3000:3000"
#     volumes:
#     - .:/code
#     - logvolume01:/var/log
#     links:
#     - redis
#   redis:
#     image: redis
# volumes:
#   logvolume01: {}

version: '3'
services:
  nodejs_app:
    container_name: nodejs_app
    restart: always
    build: .
    ports:
      - '3000:3000'
    links:
      - mongodb
  mongodb:
    container_name: mongodb
    image: mongodb
    ports:
      - '27017:27017'
